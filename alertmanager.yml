route:
  receiver: 'mail'
  repeat_interval: 4h
  group_by: [ alertname ]


receivers:
  - name: 'mail'
    email_configs:
      - smarthost: 'smtp.gmail.com:587'
        auth_username: 'gautam_sawan@infoobjects.com'
        auth_password: 'evwn ukel dkle pxbh' 
#"${{ secrets.EMAIL_PASSWORD_DEV }}"
        from: 'gautam_sawan@infoobjects.com'
        to: 'gautam_sawan@infoobjects.com'
        send_resolved: true
        html: |
          <html>
          <body>
            <h2>[ALERT] {{ .labels.alertname }}</h2>
            <p>status: {{ .status.state }}</p>
            <p>Triggered At: {{ .startsAt }}</p>
            <p><strong>Labels:</strong></p>
            <ul>
              <li><strong>Alert Name:</strong> {{ .labels.alertname }}</li>
              <li><strong>Instance:</strong> {{ .labels.instance }}</li>
              <li><strong>Job:</strong> {{ .labels.job }}</li>
              <li><strong>Severity:</strong> {{ .labels.severity }}</li>
            </ul>
            <p><strong>annotations:</strong></p>
            <ul>
              <li><strong>Description:</strong> {{ .annotations.description }}</li>
              <li><strong>Summary:</strong> {{ .annotations.summary }}</li>
            </ul>
            # <p><strong>Additional Information:</strong></p>
            # <ul>
            #   <li>Alert ID: {{ .AlertID }}</li>
            #   <li>Alert Rule: {{ .Rule }}</li>
            # </ul>
          </body>
          </html>
